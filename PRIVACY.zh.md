# DSH Desktop 企业版 — 隐私政策

[English](PRIVACY.md)

- **版本：** 2.0（企业部署版）
- **生效与最后更新：** 2026 年 9 月 1 日

本政策适用于 DSH Desktop **企业构建**——DSH Enterprise 平台桌面客户端的内网自部署形态。对本构建而言，本政策取代上游社区隐私政策：上游社区发行版与 `dshdesktop.cn` 在线服务**不参与**本文所述的任何数据流。

本文中，"我们"指部署并运营本构建的组织（即贵企业平台运营方）。某次部署的个人数据控制者是该组织；相关问询与权利请求请通过贵组织内部渠道联系管理员，而非任何上游社区渠道。

## 1. 范围

本政策覆盖由本仓库构建、并配置为对接组织自建网关的桌面客户端，说明：

- 客户端的**数据面**：客户端与企业网关交换什么、何时交换；
- 客户端的**外联清单**：本构建可能触达的每一个网络目的地及其默认状态；
- 客户端在工作站上的**本地数据**。

## 2. 摘要

- 全部模型与工具流量发往**贵组织的网关**（预置的 `DSH_ENTERPRISE_GATEWAY_URL`）。本构建不配置任何第三方模型服务。
- **更新检查默认关闭。** 不存在任何能到达上游公共更新服务的生产代码路径：更新源预置解析器对该域名及其子域直接拒绝，且版本检查与安装包下载仅在部署方预置自建更新源（`DSH_ENTERPRISE_UPDATE_URL`）后才会运行；否则不发出任何更新相关请求。
- **社区市场默认禁用。** 市场提供器以 `disabled` 状态启动，在显式启用前不访问任何服务。
- **遥测默认关闭。** 最小隐私遥测模块（客户端版本、在线状态、错误计数）已就绪但在本构建中没有任何调用点；即使部署方日后启用，上报也只发往组织网关。
- 安装 UUID 本地生成、本地保存。默认配置下**从不出网**；只有在发往组织自营端点（预置更新源或已启用遥测）时才会传出，且任何情况下都不会发往上游公共端点。
- 会话、凭据、日志与崩溃文件默认留在工作站；企业令牌密封于操作系统钥匙串。

## 3. 企业网关通信（数据面）

客户端唯一的常驻网络关系是与组织网关的通信：

| 交互 | 交换内容 | 用途 |
| --- | --- | --- |
| 登录（OAuth 2.0 + PKCE） | 系统浏览器打开组织授权端点；一次性授权码经 `127.0.0.1` 回环监听返回。客户端获得 OAuth 令牌。 | 员工身份认证；密码不会到达客户端。 |
| 令牌刷新 | 客户端在令牌半生命期向网关令牌端点出示刷新令牌。 | 免重复登录、维持会话。 |
| 身份读取 | 客户端调用网关身份/userinfo 接口。 | 在设置页呈现已登录账号（账号、显示名、角色）。 |
| LLM 出口令牌 | 客户端以会话换取网关签发的短期模型令牌，注入代理环境（`DSH_LLM_TOKEN`）。 | 使模型与工具调用经网关出口，且不暴露长期凭据。 |
| 模型与工具流量 | 代理会话所需的提示词、回复、附件与工具输入输出。 | 送达网关 LLM 接口，受组织平台策略约束。 |

与任何服务器一样，网关在每次请求中都会收到标准网络元数据（IP 地址、时间、TLS 细节）。网关能将这些数据与具体个人关联到什么程度，由组织自身的服务端政策约束，不在本文范围内。

## 4. 最小遥测（默认关闭；端点待服务端交付）

本构建内置的遥测上报器只含以下字段，别无其他：

- `clientVersion`：运行中的客户端版本；
- `online`：客户端当前能否连通网关；
- `errorCounts`：有界的按类错误计数（标签仅限字母、数字、点、下划线、连字符；至多 32 类，计数饱和）；
- 外加安装 UUID 与 UTC 时间戳。

在本构建中，该上报器处于**休眠态**：没有任何代码构造或配置它，因此不存在遥测出网。接收端（网关上的 `POST /api/desktop/telemetry`）由治理服务端工作流交付；只有该端点就绪且部署方显式开启上报后，上报才会开始——且只发往组织网关。上游会话遥测（`DSH_TELEMETRY_MODE`）在默认组合中保持 `DISABLED`；若运营方显式设置，该流量遵循上游配置与接收方政策。

## 5. 更新检查与安装 UUID（默认关闭；仅限自建源）

- 未预置更新源时，客户端不注册更新托盘项、不注册更新路由、无后台轮询——**更新相关出网为零**。预置值非法时同样按关闭处理（记录原因后禁用）；命名上游公共更新域名（`dshdesktop.cn` 或其任意子域）的预置会被以专门理由直接拒绝。不存在回退到任何公共端点的路径。
- 部署方预置自建源后，客户端周期性请求 `GET {origin}/api/desktop/version`，并在用户逐次显式确认后从 `{origin}/api/downloads/mac` 或 `/windows` 获取安装包。版本请求携带客户端版本头与安装 UUID；下载请求不携带。格式即上游版本元数据契约（`{"version": "<稳定 SemVer>"}`，≤ 4 KiB）；签名校验是规划中的服务端（T17）扩展——当前客户端尚不要求签名字段。
- 安装 UUID 是本地生成的随机 UUID v4，持久保存于 Electron 用户数据目录（`identity/installation-id`，受限文件权限），仅在文件缺失或损坏时重新生成，绝不派生自硬件或账号标识。它标识的是一个用户数据目录，而非一台物理机器。

## 6. 外联清单（默认状态）

| 目的地 | 默认状态 | 何时可能被访问 |
| --- | --- | --- |
| 组织网关 | 即上文数据面 | 用户登录后始终存在。 |
| 自建更新源 | **关闭**——未预置 | 仅当部署方预置 `DSH_ENTERPRISE_UPDATE_URL`。 |
| 社区市场源（npm registry、GitHub raw、目录 URL） | **禁用**——提供器以 `disabled` 启动 | 仅当用户显式选择提供器；部署方可将 npm 与目录指向内部镜像（见 `docs/enterprise-self-hosting.md`）。 |
| `dshdesktop.cn` 或任何上游公共服务 | **从不** | 没有生产代码路径能到达它：更新源预置解析器对该域名及其子域直接拒绝，且运行时在未预置源点时拒绝下载。 |
| 上游会话遥测端点 | `DISABLED` | 仅当运营方显式设置 `DSH_TELEMETRY_MODE`。 |
| npm registry、依赖承载方与 Electron Node 头文件服务（`https://electronjs.org/headers`） | **运行期按需** | 当 profile 依赖在启动时物化，或你执行 pnpm/终端操作时，内置包管理器可能访问它们。Electron 头文件地址经 `npm_config_disturl` 固定；Electron 二进制下载本身遵循标准 `ELECTRON_MIRROR` 环境变量，部署方可将其预置为内网镜像（物化子进程继承部署方环境）。未发生依赖物化或包操作时不访问。 |

## 7. 本地数据

| 数据 | 位置 | 说明 |
| --- | --- | --- |
| 会话、提示词、回复、工具记录、附件 | `$DSH_HOME/sessions`、`$DSH_HOME/attachments/v1` | 本地优先；仅在你调用模型或工具时内容才发往网关。 |
| 配置档、设置、机器补丁 | `$DSH_HOME`（含 `cordis.patch.yml`） | 本地配置，删除前一直保留。 |
| 企业 OAuth/LLM 令牌 | 操作系统钥匙串（经 Electron safeStorage） | 静态加密密封；本构建不会将其以明文文件落盘。 |
| 旧式本地凭据 | `$DSH_HOME/.credentials.yaml`（`0600` 文件、`0700` 目录） | 未加密；同操作系统用户的进程可以读取。 |
| 安装 UUID | `<用户数据>/identity/installation-id` | 仅本地，见第 5 节。 |
| 更新与市场状态 | `<用户数据>/updates/state.json`、`desktop-market/state.json` | 记账信息（上次检查、提供器选择）；市场状态以 `disabled` 起始。 |
| 日志 | Electron 用户数据目录下 | 10 MiB 轮转、7 天清理、200 MiB 上限；可能含路径、会话 ID、命令。 |
| 崩溃文件 | 本地 Crashpad 存储 | 仅本地收集；**未配置崩溃上传**。 |
| 诊断归档 | 仅在你导出时创建 | 可能含日志与标识；分享前请先行审阅。 |

卸载应用可能遗留上述文件；工作站转作他用前请删除 DSH home 与用户数据目录。

## 8. 浏览器与局域网访问、本地 HTTP 面

允许 DSH 在浏览器中打开，即允许该浏览器访问本机上的 Host；回环访问默认限于 `127.0.0.1`。上游以"浏览器信任标记 + 持久浏览器会话 Cookie"为配置面设防。本构建在此之上叠加**企业会话层**：当企业会话存在时，配置与管理类请求还必须携带当前有效的企业会话（fail-closed，仅明确有效才放行）；登出与重新认证窗口作为恢复路径豁免。启用局域网访问仍会使局域网内客户端可以操作会话，应只在可信网络上临时开启。

## 9. 你的选择

- 登出以使本地会话失效并清除登录态。
- 除非部署方预置了更新源，更新检查保持关闭；本构建有意不提供用户侧开关。
- 除非组织在服务端交付后显式开启，遥测保持关闭。
- 本地数据可按第 7 节清除；删除安装 UUID 后下次启动会生成新值。

## 10. 联系与变更

隐私问题、数据主体请求或保留期问询，请通过内部渠道联系**贵组织的部署管理员**。上游社区渠道与服务不参与本构建，无法代为答复。

当数据类别、接收方或默认状态发生实质变更时，本政策将在仓库历史中以新版本号与生效日期更新。中英文版本具有同等效力；若两者存在分歧，请对照阅读并向管理员报告差异。
